import React from 'react';

const Toast = () => {
    return (
        <div id="toast"
            className="fixed bottom-5 left-1/2 -translate-x-1/2 px-5 py-3 rounded-lg text-white text-sm font-medium shadow-lg hidden transition-all duration-300">
        </div>
    );
};

export default Toast;
